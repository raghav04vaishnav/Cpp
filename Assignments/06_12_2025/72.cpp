Take two integers as input and check if the first is greater than the second, then print "True" or "False".
Ans :- #include<bits/stdc++.h>
        using namespace std;

        int main() {
        int a, b;
        cout << "Enter two integers: ";
        cin >> a >> b;
        if (a > b)
        cout << "True";
        else
        cout << "False";
        return 0;
    }
