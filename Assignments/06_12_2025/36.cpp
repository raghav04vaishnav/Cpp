Print "I prefer __ over __" where two preferences are entered by the user. 
Ans :- #include <bits/stdc++.h>
        using namespace std;
        int main() {
        string first, second;
        cout << "Enter your first preference: ";
        cin >> first;
        cout << "Enter your second preference: ";
        cin >> second;
        cout << "I prefer " << first << " over " << second << '\n';
        return 0;
       }

